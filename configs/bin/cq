#!/usr/bin/env bash

[[ -z "$1" ]] && printf "Usage: %s <file-or-directory>\n" "$0" && exit 1
[[ ! -e "$1" ]] && printf "Error: '%s' does not exist.\n" "$1" && exit 1
xattr -rd com.apple.quarantine "$1"
printf "Quarantine attribute removed from: %s\n" "$1"
