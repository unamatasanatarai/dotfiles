#!/usr/bin/env bash

set -euo pipefail

notes_file="$HOME/Documents/notes.md"

if [ $# -gt 0 ]; then
  echo -e "\n[ ] " "$@" >>"$notes_file"
  echo "I added your note to ${notes_file}"
  exit 0
fi
nvim -c "norm Gzz" "$notes_file"
