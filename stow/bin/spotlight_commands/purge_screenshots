#!/usr/bin/env bash

set -euo pipefail

. "$(dirname "$0")"/../.lib

sdir="${HOME}/Pictures/screenshots"

sdu=$(du -h "$sdir")
sls=$(("$(find "$sdir" | wc -l)" - 1))

einfo -n "purging ${sls} files ${sdu}"
rm -rvf "${sdir:?}"/*
eok ""
