#!/usr/bin/env bash

set -euo pipefail

declare -A commands=(
  ["Brave"]="open -a 'Brave Browser'"
)

choices=("${!commands[@]}")

cmd=$(printf "%s\n" "${choices[@]}" | peco --prompt="►▶ ")

[[ -z "$cmd" ]] && exit 1

eval "${commands["$cmd"]}"

